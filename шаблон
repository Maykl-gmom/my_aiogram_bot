# --- Імпорти бібліотек ---
# тут підключи aiogram (Bot, Dispatcher, types, InlineKeyboardMarkup, InlineKeyboardButton)
# також додай FSM модулі (State, StatesGroup, FSMContext)
# і asyncio для запуску

# --- Токен ---
# створи змінну для токена бота

# --- Ініціалізація бота і диспетчера ---
# створи об'єкт бота
# створи об'єкт диспетчера

# --- Клавіатури ---
# створи головну клавіатуру з двома кнопками: "Каталог" і "Оплата"

# --- Класи станів (FSM) ---
# створи клас для станів замовлення з двома кроками:
# choosing_product
# choosing_payment

# --- Обробник команди /start ---
# напиши функцію, яка відправляє привітання і прикріплює клавіатуру

# --- Обробник кнопок ---
# напиши функцію, яка реагує на "catalog" і "payment"
# при "catalog" — зчитує вміст products.txt і відправляє його
# при "payment" — відправляє текст з реквізитами
# в обох випадках змінює стан користувача на відповідний

# --- Обробник стану choosing_product ---
# напиши функцію, яка після вибору товару переводить у choosing_payment

# --- Обробник стану choosing_payment ---
# напиши функцію, яка завершує замовлення і очищає стан

# --- Запуск бота ---
# створи async main(), в якому запускаєш dp.start_polling(bot)
# додай перевірку if __name__ == "__main__" і asyncio.run(main())
